
export const TextSizes = {
    xs: "text-[clamp(0.25rem,0.5rem+1vw,0.75rem)]",
    sm: "text-[clamp(0.5rem,0.75rem+1vw,1rem)]",
    md: "text-[clamp(0.75rem,1rem+1vw,1.5rem)]",
    lg: "text-[clamp(1.5rem,2rem+2vw,2.5rem)]",
    xl: "text-[clamp(2rem,2.5rem+2vw,3rem)]",
}